/**
 * SandboxService — DEPRECATED
 *
 * Code execution has been moved entirely to Pyodide running client-side.
 * Exercise test cases are validated in the browser via the usePyodide hook,
 * and only the results (pass/fail, expected/actual output) are sent to the
 * server for persistence. No third party ever sees the learner's code.
 *
 * This file is kept as a stub for documentation purposes. If you need
 * server-side execution in the future, deploy your own Piston or Docker
 * sandbox — do NOT point this at the public emkc.org API.
 */

// The public Piston API URL is intentionally not exported anymore.
// const PISTON_API = "https://emkc.org/api/v2/piston/execute";
